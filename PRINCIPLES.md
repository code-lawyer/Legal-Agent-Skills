# 法律 Skill 最高原则（作者宪章）

> 本文件是**我们制作 Legal-Agent-Skills 时遵守的准绳**，约束的是作者，不是终端 agent。
> 它**不写进任何 `SKILL.md` 正文，agent 运行时不读取它**。造 skill、评审 PR、
> 决定"某段内容值不值得写"时对照本宪章。
>
> 原则分四层：**元层**（关于宪章与作者自身）→ **工程/架构纪律** → **法律专业质量底线**
> → **产品/交付体验**。每条写成 **断言 / 为什么 / 怎么落地**，落地项尽量对应到一个
> 可执行的强制函数或自查动作，而非口号。

---

## 变更日志

> 宪章随实践演进，但改动留痕：日期 + 一句话 + 起因。

- **2026-08-15** — 初版立宪：元层 M1–M3、工程 E1–E7、法律 L1–L6、产品 P1–P5。
  起因：项目进入"先立最高原则、再据此构造一系列法律 skill"的新阶段。
- **2026-08-17** — E3 增补 rubric-blind test + 3-variant test 两个作者自查（防个案泄漏 / 防过拟合）。
  起因：GitHub 竞品调研发现 Parthenon Law（Harvey-labs，1251 skill）的 "framing-first, not rubric
  reverse-engineering" 原则与本宪章 E3/anti-leakage 同源，其两个自查测试可直接落地。
- **2026-08-17** — L1 增补 Mode 1 / Mode 2 两态幻觉框架（核验不止验存在，更要验逐字命中 / 确被支撑）。
  起因：竞品 master-claude-for-legal 的 citation-verifier 指出 Mode 2（来源真实但转述错误）是致命且常被忽略的失败态。

---

## 元原则（关于宪章与我们自己）

### M1. 宪章约束作者，不进 skill 运行时。
这些原则是我们做 skill 时的准绳，**不写进任何 SKILL.md 正文**，agent 不读。
- **为什么**：原则若混进 skill 正文，会稀释运行时的常驻底线、浪费 token，也让"给作者看的"
  和"给 agent 执行的"两类内容纠缠不清。
- **落地**：宪章放仓库顶层（本文件）；skill 目录内不引用它。

### M2. 原则须可执行、可自查，不是口号。
每条原则能对应到一个检查动作或强制函数，否则就是装饰。
- **为什么**：不能被检查的原则等于没有原则。
- **落地**：凡能机器校验的，进各 skill 的 `validate.py`；凡靠人判断的，写成 PR 自查清单一行。

### M3. 宪章随实践演进，但改动留痕。
市面调研或造 skill 过程中发现某条原则需要修订，就回来改——但记录为什么改。
- **为什么**：宪章是活的；无痕修改会让"当初为何这样定"的判断丢失。
- **落地**：维护上方《变更日志》，每次改动追加一行。

---

## 第一层：工程 / 架构纪律

### E1. 自包含、可单独下载、自定位。
每个 skill 是独立交付单元，**零依赖本库其他 skill**；内部所有路径相对 `SKILL.md` 解析，
与当前工作目录无关。
- **为什么**：使用者要能单独拿走任意一个 skill 就用，绝不搞"必须整库一起才能跑"。
- **落地**：禁止跨 skill 相对引用；`validate.py` 检查 SKILL.md 内引用不越出本 skill 目录；
  多个 skill 需要相同内容时"各自复制一份"，不建共享底座。

### E2. 渐进式披露。
`SKILL.md` 只放**常驻底线 + 按需读取索引**；正文（流程 / 方法 / 规则 / 模板 / 终检门）
分层进 `references/`、`rules/`，运行时分阶段按需读取。
- **为什么**：既省 token，又保证深层规则不会因为塞在一个长文件里而被跳过。
- **落地**：SKILL.md 设行预算上限；validate 检查必备文件 / 标题 / 无悬空引用。

### E3. 只补模型做不到 / 会跳过的，不重复提示原生能力。
不 spell out 模型本就具备的法学方法论（演绎三段论、目的解释、体系解释、类推、IRAC、
论证结构自查……）。skill 的价值只在五处：
①对真实来源的核验 / 防幻觉；②权重里不可靠的领域事实（具体条号 / 阈值 / 最新司法解释
状态 / 法域强制条款）；③对易跳步骤的强制函数（来源标签、终检门、回退）；④交付物的格式
与法域行文惯例；⑤工具编排（MCP 探测、红线渲染等）。
- **为什么**：重复教模型已会的事只是浪费 token、稀释真正重要的纪律。
- **落地**：评审每段内容时先问"这是在教方法论（原生→砍掉），还是在补①–⑤（→保留）？"
- **落地（防个案泄漏 / 防过拟合，两个自查测试）**：skill 内容必须是**某一类工作的通用指引**，不是某个个案/样本的作弊小抄。写每段内容时过两关（借鉴 Parthenon Law 的 rubric-blind 原则）——
  - **rubric-blind test**：这句话如果**只因为我读了这个具体个案/样本（或某评测标准答案）才写得出来**，就删掉或泛化到"类"。个案的金额/主体/逐字引文/具体法条号应交由运行时从真实来源提取（对应②领域事实交 MCP、L1 防幻觉），不硬编码进 skill。
  - **3-variant test**：把当事人 / 法域 / 金额 / 具体条文换成同类的三个变体，这段内容还成立吗？换掉就崩，说明它过拟合了某个实例，应上移到类别层。

### E4. 强制函数优于劝诫。
关键纪律做成**可执行的门**（`validate.py` / 终检门 / 来源标签 / plan 预检），
而不是"请记得……"。
- **为什么**：模型会跳过软性叮嘱；能让违规失败可见的机制才可靠。
- **落地**：每条"必须做"的纪律，优先为它找一个能让违规显性失败的机制。

### E5. 结构可机器校验。
每个 skill 自带 `validate.py`（仅标准库），把结构漂移堵在提交前——行预算、必备文件 /
标题、无悬空引用、登记表↔卡文件双向一致、SKILL.md 硬不变量锚点等。
- **为什么**：结构是渐进式披露的地基，地基漂移则规则被跳过。
- **落地**：CI 门禁跑 `validate.py` + 单元测试，全绿才允许合并。

### E6. 跨 agent 可移植。
面向"能读文件、能遍历目录"的通用文件系统 agent（Claude Code / Codex / Cursor / Cline /
Copilot CLI / Gemini CLI 等），不绑定任一 agent 的私有机制。
- **为什么**：绑死单一宿主会让 skill 失去可移植性。
- **落地**：唤起说明覆盖多种 agent；不假设某家专有的 skill API；纯聊天无文件系统的 agent
  明确列为不支持。

### E7. 工具可插拔、运行时实测、缺失显性降级。
不写死任何具体 MCP / 工具名；运行时**发真实探测调用**（不只看配置）；工具缺失时按预定
路径明确降级并告知，**绝不静默失败**。
- **为什么**：写死工具名会让 skill 脆弱；只看配置会误判可用性。
- **落地**：探测 → 有则用、无则走预定降级分支并写清当前状态。

---

## 第二层：法律专业质量底线

### L1. 防幻觉是第一设计目标。
法条、案例、裁判规则**绝不靠模型记忆**——那是最危险的幻觉源。每条法律命题**挂且仅挂一个
来源标签**，标签描述"实际来源行为"（如 `[MCP核验:…]` / `[模型知识-未验证]`）而非
"自信程度"；三轴正交（验证渠道 / 权威位阶 / 处置状态），互不冒充。
- **为什么**：错引一个条号或编造一个案号，比诚实说"我不确定"危害大得多。
- **落地**：来源标签做成强制函数；终检门检查"每条法律命题恰好一个标签"；
  `[模型知识-未验证]` 命题不得作为唯一依据。
- **落地（两态幻觉，核验不止验存在）**：区分两种幻觉并都要防（借 master-claude-for-legal）——
  **Mode 1**：引的来源**不存在**（编造案号/条号）；靠 grounding/检索基本可解。
  **Mode 2**（更阴险）：来源**真实、但不说所声称的话**（真案号 + 错误转述 / 把释义当逐字引用 / 断言未被支撑）。
  因此核验不能止于"来源存在"：凡逐字引用须**在来源处逐字命中**，凡"某某规定/某院认为…"须**确被来源支撑**
  （VERIFIED / 部分支撑但被拔高 / 不被支撑）。能机检的逐字命中应交强制函数。

### L2. 核验渠道可插拔，但"真研究"有硬前置。
区分两类 skill：核验对**方法论**是可选加速器的（如合同审查——无外部库也能跑核心方法），
与核验是**硬前置**的（如法律研究 / 类案检索——没有可靠法律库就没有真检索）。后者未接库时
先引导用户接入、暂停正式产出，唯一例外是用户**显式 opt-in** 的通篇水印降级草稿。
- **为什么**：把两类混为一谈，会要么过度阻塞、要么放行伪研究。
- **落地**：每个 skill 在 SKILL.md 常驻底线明确声明自己属于哪一类。

### L3. 不越权、不冒充最终法律意见。
产出是**基于所给材料与检索结果的初步专业分析**，不替代主办律师对具体个案的判断与签字责任。
- **为什么**：agent 产出被当成最终法律意见直接采用，是执业风险。
- **落地**：交付物带免责声明；未核验命题不得作为诉讼 / 法律交付物的唯一依据；
  提示"据此行动前经执业律师复核确认"。

### L4. 法域中立 + 法域判定显式可复现。
方法骨架法域无关，各法域规则各自成包（`rules/cn/`、`rules/us/`……）；判定法域有**明确、
单一、可复现**的规则，绝不靠猜。
- **为什么**：法域判错，整份分析的法律依据就全错。
- **落地**：如合同审查"判定法域只看法律选择条款，绝不看合同语言"；判定规则写进路由层并
  可校验；加一个新法域应做到零改动路由层。

### L5. 遵循法律专业行文惯例（house-style）。
交付物按对应法域 / 文种的执业惯例成文——该用 IRAC 用 IRAC、该用请求权基础用请求权基础、
该用裁判文书体用裁判文书体，由 skill **明确选定**而非随机漂移。
- **为什么**：这是"执业级"与"像模像样"的分水岭，也正是模型权重里不稳、需要 skill 固定的部分。
- **落地**：每个 skill 在输出模板里锁定文种与 house-style。

### L6. 执业伦理与安全底线。
利益冲突意识、保密、不协助违法目的；**Token / 密钥绝不写入 skill 文件、不提交仓库、
不随报告输出**。
- **为什么**：法律工作对保密与合规的要求高于一般软件；泄密或协助违法是不可接受的失败。
- **落地**：仓库只给占位符；`SECURITY.md` 约定 Token 处理；涉敏感 / 对外产出前确认授权。

---

## 第三层：产品 / 交付体验

### P1. 触发边界互斥、彼此路由、明确"不做什么"。
每个 skill 职责单一、边界清晰；不仅说自己做什么，还**显式声明不做什么**；相邻 skill 互相
路由、不重叠、不抢活。
- **为什么**：边界糊了，agent 会用错 skill，或让一个 skill 越界做砸。
- **落地**：SKILL.md 常驻区写清"触发场景 / 不做的事 / 相邻 skill 如何路由"
  （如合同 → contract-review、法律问题 → legal-research）。

### P2. 中文法律执业级、母语行文。
面向中文法律工作者，交付物以专业中文成文，不是翻译腔。
- **为什么**：目标用户是中文执业者；行文腔调直接影响可用性与专业观感。
- **落地**：模板与示例用中文法律行文；术语采用执业习惯用语。

### P3. 失败显性化，绝不静默降级。
依赖缺失、核验失败、回退到达上限、工具不可用——**一律明确告知用户**当前处于什么降级状态、
影响是什么。
- **为什么**：在法律场景里，用户不知道自己拿到的是降级产物，本身就是风险。
- **落地**：每个降级分支都产出一句明确的状态说明；如红线缺 `python-docx` → 降级 markdown
  并告知；研究缺库 → 暂停或水印草稿并告知。

### P4. 真实工件不伪造。
生成的 Word 修订痕迹、报告、引用**必须真实可核**；做不到就**降级并告知**，绝不伪造一个
看起来对的东西。
- **为什么**：伪造的红线痕迹 / 假引用比不给更危险。
- **落地**：红线渲染真实 `w:ins`/`w:del` 修订痕迹；引用挂真实来源标签；宁可标"未核验"也不编。

### P5. 交付物自带质量门与诚实的不确定性标注。
产出前过终检门；不通过按**精确回退**补正，回退设**上限**防死循环；到达上限时**诚实标注
不确定性优于卡死不交付**。
- **为什么**：既要挡住不合格产出，又不能因追求完美而卡死不交付。
- **落地**：如 legal-research 的 QC 七项终检门 + 精确回退目标 + 回退次数上限。

---

## 如何使用本宪章

- **造新 skill 时**：先通读本宪章，再动手；设计阶段逐条自检是否满足。
- **评审 PR 时**：以四层原则为清单；违反 E/L/P 的任一条都应在合并前解决或显式记录例外。
- **"取长补短"吸收市面 skill 时**：先用 E3 过滤——对方在教模型原生方法论的部分一律不吸收，
  只吸收补足①–⑤（核验 / 领域事实 / 强制函数 / 格式行文 / 工具编排）的部分。
- **发现原则需要修订时**：改本文件，并在《变更日志》追加一行说明起因。
