# Roadmap / 真实进度看板

> 本文件是当前**真实**进度与后续计划的唯一看板。早期的 specs / plans 历史实施计划
> 已完成其使命，随上一阶段开发收尾一并清理，进度以本文件为准。

## 已完成（v1.1.0 + 评审后加固）

- contract-review：中美双法域审查、可插拔规则包、双轴风险清单、真实 Word 红线稿。
- legal-research：研究/检索/复合三路径、MCP 硬前置、来源标签三轴、QC1–QC7 终检门。
- document-fill：模板 × 本案知识库 锚定填充，Mode-2 闸门。
- 工程：`validate.py` 结构校验、打 tag 自动打包发布。
- 评审后加固：
  - [x] 修复同段落多修订红线缺陷（+ 回归测试）。
  - [x] push/PR CI：`validate.py` + `pytest` 门禁。
  - [x] 红线计划 `validate_plan()` 预检门（必填字段 + 动作合法性）。
  - [x] 治理文件：LICENSE(MIT) / CHANGELOG / CONTRIBUTING / SECURITY。

## 待办（按优先级）

- [ ] **端到端效果评测（评审 #3）** — 需真实法律内容，不可自动编造。建脱敏黄金样本
  集（中美合同各 5–10 份、研究问题 10–20 个），评测遗漏率、误报率、来源标签合规率、
  法域路由准确率。**待提供/复核真实样本与标准答案后开工。**
- [ ] **美国法领域卡补齐（评审 #5）** — 现 US 3 卡（买卖/SaaS/NDA）对 CN 5 卡不对称；
  标准条款库亦为中国法版本。补 US 领域卡与条款需美国法执业审阅，**不自动生成。**
- [ ] 红线脚本加固测试：重复锚点、表格单元格、超链接、跨多运行的锚点、已有修订痕迹
  叠加（后者已有一例）。
- [ ] 依赖收敛：评估是否引入统一 `pyproject.toml`（当前用 `requirements*.txt`）。
- [ ] 将 `contract-review/scripts/redline_docx.py` 的红线计划字段整理为显式 JSON
  Schema 文档（现有 `validate_plan()` 已做运行时校验）。

## 设计原则备忘

- skill 只补模型**做不到/会跳过**的部分（核验、领域事实、强制函数、格式、工具编排）；
  模型原生具备的法学方法论**不重复提示**。
- 防幻觉是第一设计目标：法条/案例须挂来源标签，未核验不得作为唯一依据。
